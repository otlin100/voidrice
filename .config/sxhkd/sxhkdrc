super + Return
    $TERMINAL
super + shift + Return
    samedir
super {_,+ shift, + alt} + period
    anoise {toggle,shuffle,volume}
super + d
    via
super + shift + d
    restartDiscord
super + m
    spotify
super + v
    $TERMINAL -e nvim_mru
super + alt + b
    bluetooth_connect
super + alt + k
    fontsize +0.5
super + alt + j
    fontsize -0.5
super {_,+ shift} + c
    clipmenu.sh {show,clean}
super + z
    camtoggle
super + acute
    dmenuunicode
super + e
    $TERMINAL -e neomutt
super + shift + r
    winresize
super + shift + { z, o, i, u }
    i3resize { left, right, up, down }
super + n
    $TERMINAL -e newsboat
super + s
    skypeforlinux
super + shift + s
    skypelogin
super + F1
    groff -mom $XDG_DATA_HOME/larbs/readme.mom -Tpdf | zathura -
super + F3
    displayselect
super + shift + F3
    arandr
super + F5
    sudo -A systemctl restart NetworkManager
super + F6
    torwrap
super + F7
    td-toggle
super + F8
    mailsync
super + F9
    dmenumount
super + F10
    dmenuumount
super + F11
    ducksearch
super + F12
    networkmanager_dmenu
super + plus
    light -A 3
super + minus
    light -U 3
super + p
    password
super + t ; { s, h }
    telearbeit { sipgate, hhu }
super + shift + p
    lmc pause
super + alt + { space, n, p }
    playerctl { play-pause, next, previous }
super + alt + c
    showTrackInfo
super + {_, alt } + i
    $TERMINAL -e { htop, ytop }
super + a
    $TERMINAL -e pulsemixer
super + r
    launch_lf
super + shift + r
    remaps
super + shift + e
    tutorialvids
super + w
    $BROWSER
super + Insert
    showclip
super + Pause
    cabl
super + x ; {s,h,r,l,w,x,n}
    turnoff {suspend,hibernate,reboot,lock,logout,shutdown,network}
Print
    maim pic-full-"$(date '+%d%m%y-%H%M-%S').png"
shift + Print
    maimpick
super + Print
    dmenurecord
super + Scroll_Lock
    "killall screenkey || screenkey"
super + Delete
    dmenurecord kill
XF86AudioMute
    lmc mute
XF86AudioLowerVolume
    lmc down 5
XF86AudioRaiseVolume
    lmc up 5
