#!/bin/sh

secs=1
endTime=$(( $(date +%s) + secs ))

while [ "$(date +%s)" -lt $endTime ]; do
    killall discord electron
done

case $1 in
    restart)
        discord ;;
esac

pkill -RTMIN+17 dwmblocks
