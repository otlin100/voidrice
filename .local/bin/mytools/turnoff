#!/bin/sh

case "$1" in
    reboot)
        sudo systemctl reboot ;;
    shutdown)
        myshutdown ;;
    suspend)
        sudo systemctl suspend ;;
    hibernate)
        sudo systemctl hibernate ;;
    lock)
        slock ;;
esac
